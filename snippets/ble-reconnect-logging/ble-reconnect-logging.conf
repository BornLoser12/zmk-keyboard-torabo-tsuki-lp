# Keep the USB diagnostic image small enough for the BMP Boost UF2 bootloader.
CONFIG_ZMK_LOG_LEVEL_WRN=y
CONFIG_LOG_DEFAULT_LEVEL=2
CONFIG_LOG_BUFFER_SIZE=2048

# The diagnostic CDC port is dedicated to logging.
CONFIG_ZMK_CDC_ACM_BOOTLOADER_TRIGGER=n
